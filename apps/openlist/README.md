# OpenList for fnOS

OpenList 是一个支持多种存储后端的文件列表/WebDAV 程序，是 AList 的社区分支。

## 特性

- 支持多种存储后端（本地存储、阿里云盘、OneDrive 等）
- WebDAV 服务器
- 文件预览和在线播放
- 默认端口：5244

## Local Build

```bash
cd apps/openlist && bash ../../scripts/build-fpk.sh . app.tgz
```
